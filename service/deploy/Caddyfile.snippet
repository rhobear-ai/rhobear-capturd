# Add to rhobear-vps Caddy. Domain (G2) → this box; Caddy gets the cert automatically.
capturd.rhobear.ai {
    encode zstd gzip
    # video responses can be large; don't buffer them
    reverse_proxy 127.0.0.1:8099 {
        flush_interval -1
    }
}
