# Captur'd hosted service — API layer.
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27

# The render worker shells the Captur'd engine (film.py). The host/container
# also needs the engine + its runtime:
#   pip install "capturd[voice]"   (from the sunsponge-capture repo/wheel)
#   playwright install chromium
#   ffmpeg on PATH (or bundled at packaging/ffmpeg/)
# See DEPLOY.md.
